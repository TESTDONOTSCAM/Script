game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("SetTeam","Marines")

local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

local function MakeDraggable(topbarobject, object)
 local Dragging = nil
 local DragInput = nil
 local DragStart = nil
 local StartPosition = nil

 local function Update(input)
 local Delta = input.Position - DragStart
 local pos = UDim2.new(StartPosition.X.Scale, StartPosition.X.Offset + Delta.X, StartPosition.Y.Scale, StartPosition.Y.Offset + Delta.Y)
 local Tween = TweenService:Create(object, TweenInfo.new(0.15), {
  Position = pos
 })
 Tween:Play()
 end

 topbarobject.InputBegan:Connect(
  function(input)
  if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
  Dragging = true
  DragStart = input.Position
  StartPosition = object.Position

  input.Changed:Connect(
   function()
   if input.UserInputState == Enum.UserInputState.End then
   Dragging = false
   end
   end
  )
  end
  end
 )

 topbarobject.InputChanged:Connect(
  function(input)
  if
   input.UserInputType == Enum.UserInputType.MouseMovement or
  input.UserInputType == Enum.UserInputType.Touch
  then
  DragInput = input
  end
  end
 )

 UserInputService.InputChanged:Connect(
  function(input)
  if input == DragInput and Dragging then
  Update(input)
  end
  end
 )
end

_G.HubName = "ScriptBlox | Chest Farm"
_G.YTName = "ScriptBlox"
_G.Color = Color3.fromRGB(255,0,0)

local LibChest = Instance.new("ScreenGui")

LibChest.Name = "LibChest"
LibChest.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
LibChest.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local Frame = Instance.new("Frame")
local Stroketime1 = Instance.new("UIStroke")

Frame.Parent = LibChest
Frame.AnchorPoint = Vector2.new(0.5, 0.5)
Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Frame.BorderColor3 = Color3.fromRGB(0, 0, 0)
Frame.BorderSizePixel = 0
Frame.Position = UDim2.new(0.5, 0, 0.5, 0)
Frame.Size = UDim2.new(0, 160, 0, 170)

MakeDraggable(Frame)

Stroketime1.Thickness = 1
Stroketime1.Name = ""
Stroketime1.Parent = Frame
Stroketime1.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
Stroketime1.LineJoinMode = Enum.LineJoinMode.Round
Stroketime1.Color = _G.Color
Stroketime1.Transparency = 0

local ImageLabel = Instance.new("ImageLabel")
local Ps = game:GetService("Players")
local P = Ps.LocalPlayer
local Stroketime2 = Instance.new("UIStroke")

ImageLabel.Parent = Frame
ImageLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ImageLabel.BackgroundTransparency = 1.000
ImageLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
ImageLabel.BorderSizePixel = 0
ImageLabel.Position = UDim2.new(0.0409999862, 0, 0.292176366, 0)
ImageLabel.Size = UDim2.new(0, 65, 0, 70)
ImageLabel.Image = Ps:GetUserThumbnailAsync(P.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)

Stroketime2.Thickness = 1
Stroketime2.Name = ""
Stroketime2.Parent = ImageLabel
Stroketime2.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
Stroketime2.LineJoinMode = Enum.LineJoinMode.Round
Stroketime2.Color = _G.Color
Stroketime2.Transparency = 0

local UICorner = Instance.new("UICorner")

UICorner.Parent = ImageLabel

local TextLabel = Instance.new("TextLabel")

TextLabel.Parent = Frame
TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.BackgroundTransparency = 1.000
TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
TextLabel.BorderSizePixel = 0
TextLabel.Position = UDim2.new(0, 0, 0.0647058859, 0)
TextLabel.Size = UDim2.new(0, 159, 0, 25)
TextLabel.Font = Enum.Font.SourceSans
TextLabel.Text = _G.HubName
TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.TextSize = 16.000

local UICorner_2 = Instance.new("UICorner")

UICorner_2.Parent = Frame

local ImageYT = Instance.new("Frame")
local Stroketime3 = Instance.new("UIStroke")

ImageYT.Name = "ImageYT"
ImageYT.Parent = Frame
ImageYT.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ImageYT.BackgroundTransparency = 1.000
ImageYT.BorderColor3 = Color3.fromRGB(0, 0, 0)
ImageYT.BorderSizePixel = 0
ImageYT.Position = UDim2.new(0.541000009, 0, 0.291999996, 0)
ImageYT.Size = UDim2.new(0, 65, 0, 70)

Stroketime3.Thickness = 1
Stroketime3.Name = ""
Stroketime3.Parent = ImageYT
Stroketime3.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
Stroketime3.LineJoinMode = Enum.LineJoinMode.Round
Stroketime3.Color = _G.Color
Stroketime3.Transparency = 0

local UICorner_3 = Instance.new("UICorner")

UICorner_3.Parent = ImageYT

local YT = Instance.new("ImageButton")

YT.Name = "YT"
YT.Parent = ImageYT
YT.Active = false
YT.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
YT.BackgroundTransparency = 1.000
YT.BorderColor3 = Color3.fromRGB(0, 0, 0)
YT.BorderSizePixel = 0
YT.Position = UDim2.new(0, 1, 0, 1)
YT.Selectable = false
YT.Size = UDim2.new(0, 30, 0, 30)
YT.Image = "rbxassetid://14764526356"
YT.MouseButton1Click:Connect(function()
	game:GetService("StarterGui"):SetCore("SendNotification", {
		Title = "ScriptBlox";
		Text = "Thanks For Subscribe";
		Icon = "http://www.roblox.com/asset/?id=14355269935";
		Duration = 10
	})
	setclipboard("https://www.youtube.com/c/ScriptBloxChannel")
end)

local DC = Instance.new("ImageButton")

DC.Name = "DC"
DC.Parent = ImageYT
DC.Active = false
DC.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
DC.BackgroundTransparency = 1.000
DC.BorderColor3 = Color3.fromRGB(0, 0, 0)
DC.BorderSizePixel = 0
DC.Position = UDim2.new(0, 34, 0, 1)
DC.Selectable = false
DC.Size = UDim2.new(0, 30, 0, 30)
DC.Image = "rbxassetid://14764624462"
DC.MouseButton1Click:Connect(function()
	game:GetService("StarterGui"):SetCore("SendNotification", {
		Title = "ScriptBlox";
		Text = "Join My Discord";
		Icon = "http://www.roblox.com/asset/?id=14355269935";
		Duration = 10
	})
	setclipboard("https://discord.gg/85gXUG58eW")
end)

local FPS = Instance.new("TextLabel")

FPS.Name = "FPS"
FPS.Parent = ImageYT
FPS.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
FPS.BackgroundTransparency = 1.000
FPS.BorderColor3 = Color3.fromRGB(0, 0, 0)
FPS.BorderSizePixel = 0
FPS.Position = UDim2.new(0.0700000003, 0, 0.600000024, 0)
FPS.Size = UDim2.new(0, 56, 0, 19)
FPS.Font = Enum.Font.SourceSans
FPS.TextColor3 = Color3.fromRGB(255,255,255)
FPS.TextSize = 16.000

spawn(function()
	while wait() do
		pcall(function()
			FPS.Text = "[ FPS ] "..string.format("%d",workspace:GetRealPhysicsFPS())
		end)
	end
end)

local YTIMAGE = Instance.new("ImageLabel")

YTIMAGE.Name = "YTIMAGE"
YTIMAGE.Parent = Frame
YTIMAGE.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
YTIMAGE.BackgroundTransparency = 1.000
YTIMAGE.BorderColor3 = Color3.fromRGB(0, 0, 0)
YTIMAGE.BorderSizePixel = 0
YTIMAGE.Position = UDim2.new(0, 24, 0.735000014, 0)
YTIMAGE.Size = UDim2.new(0, 40, 0, 38)
YTIMAGE.Image = "rbxassetid://14764526356"

local YTNAME = Instance.new("TextLabel")

YTNAME.Name = "YTNAME"
YTNAME.Parent = YTIMAGE
YTNAME.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
YTNAME.BackgroundTransparency = 1.000
YTNAME.BorderColor3 = Color3.fromRGB(0, 0, 0)
YTNAME.BorderSizePixel = 0
YTNAME.Position = UDim2.new(0.943750024, 0, 0.150774002, 0)
YTNAME.Size = UDim2.new(0, 115, 0, 25)
YTNAME.Font = Enum.Font.SourceSans
YTNAME.Text = _G.YTName
YTNAME.TextColor3 = Color3.fromRGB(255, 255, 255)
YTNAME.TextSize = 16.000
YTNAME.TextXAlignment = Enum.TextXAlignment.Left

local UIPadding = Instance.new("UIPadding")

UIPadding.Parent = YTNAME
UIPadding.PaddingLeft = UDim.new(0, 6)

wait(1)

loadstring(game:HttpGet("https://raw.githubusercontent.com/TESTDONOTSCAM/Script/main/ChestFarm"))()
